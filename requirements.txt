streamlit>=1.28
numpy>=1.24
nltk>=3.8
matplotlib>=3.7
pandas>=1.5
pymorphy3>=2.0

# Опционально: точная морфология белорусского языка (модель be_hse).
# Тянет torch, ~500 МБ. Без stanza части речи определяются суффиксными
# правилами — приблизительно, приложение об этом предупреждает.
# stanza>=1.5
